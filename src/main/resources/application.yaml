server:
  port: 8080

spring:
  application:
    name: job-tracker
  datasource:
    url: jdbc:postgresql://localhost:5432/jobtracker
    username: postgres
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true


jwt:
  secret: ${JWT_SECRET} # :default-secret-key-sadfg-sdgsdkvlm-sedfklksng-sdlkfglsrkgjn-sdlkfgsergkn
  expiration: ${JWT_EXPIRATION} # :86400000